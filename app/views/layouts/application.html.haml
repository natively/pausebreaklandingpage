!!!
%html
  %head
    %title #{title}

    -# Compass and blueprint stylesheets
    = stylesheet_link_tag 'screen', :media => 'screen, projection'
    = stylesheet_link_tag 'print', :media => 'print'
    -# Stylesheet hacks for IE8 and older
    /[if lt IE 8]
      = stylesheet_link_tag 'ie', :media => 'screen, projection'
    -# Original resets, styles, and typography stylesheets below; we can use these after we figure out a consistent typographic theme and override the Compass defaults;
    -# = stylesheet_link_tag "reset", "typography", "application",  :media => "all"


    = javascript_include_tag "application"
    = csrf_meta_tags
  %body.bp
    = yield